.panel.panel-default
    .panel-heading
        h3.panel-title.text-center 环境更换
    .panel-body
        form.form-horizontal
            .form-group
                label(for="cnapsIp").col-md-3.control-label
                    span IP地址:
                .col-md-9
                    input(type="text" value='#{serviceIP}' readonly).form-control
            .form-group
                label(for="orgIp").col-md-3.control-label
                    span 原GXP地址:
                .col-md-9
                    input(id='orgGxpIP' type="text" value='正在加载...' readonly).form-control
            .form-group
                label(for="nowIp").col-md-3.control-label
                    span 现GXP地址:           
                .col-md-9
                    select(id='GxpIP').form-control
                        each item,i in GxpIPs
                            option(value='#{item.ip}').form-control   #{item.name}   ->   #{item.ip}
            .form-group
                label(for="name").col-md-3.control-label
                    span 登记人:
                .col-md-9
                    input(id="submitName" type="text" value='').form-control
            .form-group
                label(for="name").col-md-3.control-label
                    span 备注:
                .col-md-9
                    input(id="submitNote" type="text" value='').form-control
            .form-group
                label(for="name").col-md-3.control-label
                    span 原登记人:
                .col-md-9
                    input(id="orgSubmitName" type="text" value='#{env.orgMan}' readonly).form-control
            .form-group
                label(for="name").col-md-3.control-label
                    span 原备注:
                .col-md-9
                    input(id="orgSubmitNote" type="text" value='#{env.orgNote}' readonly).form-control
            .form-group
                label(for="name").col-md-3.control-label
                    span 原登记日期:
                .col-md-9
                    input(id="orgSubmitDate" type="text" value='#{env.orgDate}' readonly).form-control
            .form-group
                label(for="name").col-md-3.control-label
                    span 登记日期:
                .col-md-9
                    -var date=new Date().getFullYear().toString()  + (new Date().getMonth()+1).toString() +  new Date().getDate().toString();
                    input(id="submitDate" type="text" value='#{date}' readonly).form-control
            .form-group
                label(for="name").col-md-3.control-label
                    span 登记人IP:
                .col-md-9
                    input(id="submitIP" type="text" value='#{env.clientIP}' readonly).form-control
            .form-group
                .col-md-offset-4.col-md-4
                    button.btn.btn-primary.btn-block(id='#{env.configId}' type="button" onclick="submitCnaps(this)")
                        span(class="glyphicon glyphicon-ok" aria-hidden="true")
                            | &nbsp
                        span
                            | 提交