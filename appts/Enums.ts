export enum SFTPType {
    put,
    get
}
export enum DeviceType {
    ICA,
    OCA,
    ALL
}

export enum FieldType {
    Req,
    Resp
}

export enum DXZPTpye {
    black,
    gray
}

export enum CoreSts {
    成功 = 1,
    失败 = 2,
    超时 = 3
}

export enum CnapsTable {
    HVPS_EXCHANGE_S = 'HVPS_EXCHANGE_S',
    HVPS_EXCHANGE_R = 'HVPS_EXCHANGE_R',
    BEPS_NOR_CREDIT_S = 'BEPS_NOR_CREDIT_S',
    BEPS_NOR_CREDIT_R = 'BEPS_NOR_CREDIT_R',
    HVPS_RTS = 'HVPS_RTS'
}